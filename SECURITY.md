# 추출 보안 설정

`api/app.py`의 기본 제한:

| 항목 | 제한 |
| --- | --- |
| API 요청 본문 | 3MiB |
| 붙여넣은 HTML / 원격 HTML | UTF-8 기준 2MiB / 다운로드 및 압축 해제 각각 2MiB |
| 추출 결과 | 4MiB |
| HTML 구조 | 요소·텍스트·주석 등 20,000개, 중첩 100단계 |
| 추출 작업 시간 | 자식 프로세스 시작 후 25초, 초과하면 프로세스 종료 및 HTTP 504 |
| 호출 빈도 | 클라이언트 주소별 분당 20회, 인스턴스 전체 분당 60회 |
| 동시 추출 | 인스턴스당 2개, 초과 요청은 대기 없이 HTTP 429 |

호출 빈도와 동시 추출 제한은 프로세스 메모리에 저장됩니다. 재시작하면 초기화되며, 여러 인스턴스나 Vercel의 콜드 스타트 사이에 공유되지 않습니다. 공개 배포에서 서비스 전체의 한도를 보장하려면 플랫폼의 WAF 또는 공유 저장소를 사용하는 제한을 추가해야 합니다. `X-Forwarded-For`를 임의로 신뢰하지 않으므로 프록시 구성에 따라 여러 사용자가 같은 주소로 집계될 수 있습니다.

추출용 자식 프로세스는 동일한 Python 및 의존성을 사용합니다. Vercel 함수의 제한은 30초로 설정해 앱의 25초 작업 종료가 먼저 일어나도록 했습니다. 로컬 테스트와 별도로 실제 배포에서 자식 프로세스 실행 및 종료, 정상 링크 추출을 확인해야 합니다.

이미지는 `ALLOWED_IMAGE_DOMAINS`에 명시된 디시인사이드 호스트의 HTTPS 주소만 남깁니다. 다른 호스트의 이미지는 제거됩니다. CSS의 URL 및 이미지 함수는 제거하며, 배경색과 기본 글자 서식은 유지합니다. 사진 OFF 결과에서는 이미지 태그도 제거합니다.

미리보기의 CSP는 이미지가 허용되지 않은 호스트로 리다이렉트되는 것도 제한합니다. Flask 응답 헤더와 Vercel에서 정적으로 제공하는 `index.html`의 meta 정책에 모두 적용합니다. 이미지 허용 목록을 변경할 때는 두 정책을 함께 갱신해야 하며, 테스트가 일치 여부를 검사합니다.

검증 명령: `python -B -m unittest discover -s tests -v`

네트워크 테스트는 DNS·소켓·TLS를 모의 처리합니다. HTML 붙여넣기와 시간 초과 테스트는 실제 자식 프로세스를 실행합니다. 외부 사이트 및 내부망에는 테스트 요청을 보내지 않습니다.
